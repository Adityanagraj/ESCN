// Deprecated: the side-by-side comparison section was removed from the site.
// This file is intentionally a no-op stub so the build keeps compiling even
// if something still imports it. Safe to delete once nothing references it.
export function CompareFrames() {
  return null
}
